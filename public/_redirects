# No explicit SPA rewrite is required. Cloudflare Pages automatically serves
# index.html for unknown client-side routes when the project has no top-level
# 404.html. API routes are restricted separately by public/_routes.json.
